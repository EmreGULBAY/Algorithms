A program that calculates binomial coefficient of an array using dynamic programming approach and a program that finds most repeated element (at least more than half of array's length) in O(nlogn) time
